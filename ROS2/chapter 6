[0720]

1. matplotlib 사용하기
import 하던 도중에 에러가 발생했음.
ImportError: numpy.core.multiarry failed to import
버전 차이 때문에 발생한 에러라고 하는데 또 구글에 나와있는 온갖 방법으로 해결이 안 됨.

[0725]

error list에 나와있는 것처럼 어찌저찌 해결을 했지만, matplotlib를 사용하려고 하면 자꾸 오류가 발생했다.
python 재설치 후, 4장 초반부에 있는 것처럼 명령들을 다시 실행하니 matplotlib가 정상적으로 작동하기 시작했다.
구글링 해보니까 버전이 안 맞아서 그런 것 같다는 의견이 많았는데 그래서 그랬던 것 같다.

plt.figure() 함수를 이용해서 그래프를 그려보는 실습을 한다.
가장 먼저 그려본 그래프는 사인함수 그래프
함수 그리는 연습도 이전에 파이썬을 배우면서 다 했던 내용이어서 간단하게 코드 작성해서 제대로 작동되는지만 확인하고 넘어감.

[0728]

실습만 함.
