24/07/18

#include <stdio.h>

int main(void) {
    int X, N, a, b, i, sum = 0;
    scanf("%d", &X);
    scanf("%d", &N);
    for (i = 0; i < N; i++){
        scanf("%d %d", &a, &b);
        sum += a*b;
    }
    if (X == sum)
        printf("Yes");
    else
        printf("No");
    return 0;
}
